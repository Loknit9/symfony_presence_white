{% extends "base.html.twig" %}


{% block title %}Accueil{% endblock %}

{% block body %}
Bienvenue sur le site!

{% if app.user %}
        <div class="mb-3">
            {# You are logged in as {{ app.user.userIdentifier }}, <a href="{{ path('app_logout') }}">Logout</a> #}
        </div>
    {% endif %}

{# Bienvenue sur le site! {{ app.user.prenom }} #}

{% endblock %}

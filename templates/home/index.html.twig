{% extends "base.html.twig" %}


{% block title %}Accueil
{% endblock %}

{% block body %}
	<div class="container-lg">

		{% if app.user %}
			<div
				class="mb-3">{# You are logged in as {{ app.user.userIdentifier }}, <a href="{{ path('app_logout') }}">Logout</a> #}
			</div>
		{% endif %}

		<div>
			<h2 class="h2"> Salut{# Salut {{ app.user.prenom }} !#}</h2>
		</div>
		<div>
			<a href="#" class="bouton_equipe">u19G2 {# {{ app.user.equipe}} !#}</a>
			<a href="#" class="bouton_equipe">GK {# {{ app.user.equipe}} !#}</a>
		</div>

	{% endblock %}


{% extends 'base.html.twig' %}

{% block title %}Hello Admin!{% endblock %}

{% block body %}
{% block content %}
    
    {# {% if app.user %}
    {% set Person = app.user.personne %}
    <h1>Bonjour Admin {{ personne.prenom }} !</h1>
    {% endif %} #}
    <h3>Admin</h3>
    <h3>Bienvenue, {{ app.user.username }}! </h3>

    <ul class="equipe_list">
        {% for equipe in equipes %}
        <div class="d-grid gap-2 col-6 mx-auto">
            <li> <button onclick="window.location.href = '#';" class="btn btn-outline-dark" type="button">{{ equipe.getCategorieAge }} {{ equipe.getCategorieGenre }} {{ equipe.getNom }}</button></li>
        </div>
        {% endfor %}
    </ul>


{% endblock %}

{% endblock %}

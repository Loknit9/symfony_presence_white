{% extends 'base.html.twig' %}

{% block nav %}
	{% include 'nav_equipe.html.twig' %}
{% endblock %}

{% block title %}Salut Admin!
{% endblock %}

{% block body %}
	{% block content %}
		<h5>Admin</h5>
		<h3>Salut,
			{{ app.user.person.prenom }}!
		</h3>
		<h5>Ajouter une équipe</h5>
		<a href="{{ path('liste_equipes') }}">
			<button type="button" class="btn add btn-danger">listes équipes</button>
		</a>
		{{ form_start(formEquipe) }}
		{{ form_widget(formEquipe) }}
		<button class="btn btn-lg btn-danger espace" type="submit" value="enregistrer">Ajouter</button>
		{{ form_end(formEquipe) }}


	</div>
{% endblock %}{% endblock %}
